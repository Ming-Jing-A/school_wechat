spring:
  application:
    name: school_wechat
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:school_wechat}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:a2791464514}
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 8GB
      max-request-size: 9GB

server:
  port: 8080
  address: 0.0.0.0
  tomcat:
    max-http-form-post-size: 9GB

mybatis:
  configuration:
    map-underscore-to-camel-case: true

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
  storage:
    local-upload-dir: ${APP_UPLOAD_DIR:uploads}
