spring:
  application:
    name: school_wechat
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:school_wechat}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:a2791464514}
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 120MB

server:
  port: 8080
  address: 0.0.0.0

mybatis:
  configuration:
    map-underscore-to-camel-case: true

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://172.16.8.73:5173,http://0.0.0.0:5173}
  storage:
    local-upload-dir: ${APP_UPLOAD_DIR:uploads}
